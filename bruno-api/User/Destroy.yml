info:
  name: Destroy
  type: http
  seq: 3

http:
  method: DELETE
  url: http://127.0.0.1:8000/api/user
  body:
    type: json
    data: |-
      {
          "phone": "+79124376562",
          "password": "secret_password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
