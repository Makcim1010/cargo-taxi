info:
  name: Show
  type: http
  seq: 3

http:
  method: GET
  url: http://127.0.0.1:8000/api/vehicles/1
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
